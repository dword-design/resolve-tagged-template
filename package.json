{
  "name": "resolve-tagged-template",
  "version": "1.0.10",
  "description": "Resolves a tagged template literal into a string. This is helpful when implementing tag functions that are not interested in the placeholders but only in the resulting string.",
  "repository": "dword-design/resolve-tagged-template",
  "funding": "https://www.buymeacoffee.com/dword",
  "license": "MIT",
  "author": "Sebastian Landwehr <info@dword-design.de>",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "checkUnknownFiles": "base checkUnknownFiles",
    "commit": "base commit",
    "dev": "base dev",
    "lint": "base lint",
    "prepare": "base prepare",
    "prepublishOnly": "base prepublishOnly",
    "test": "base test"
  },
  "dependencies": {
    "@dword-design/functions": "^2.2.2"
  },
  "devDependencies": {
    "@dword-design/base": "^7.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
